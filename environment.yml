name: ddr_est
channels:
  - conda-forge
dependencies:
  - python >=3.7
  - numpy
  - scipy
  - pandas

  # Testing infrastructure
  - pytest
  - pytest-cov

  # Tools
  - black
  - flake8
  - pdbpp
  - pre-commit
  - toml
  - tqdm

  # Plotting
  - matplotlib
  - seaborn

   # JAX
  - pip:
      - jaxlib
      - jax
      - -e ./submodules/durable/
      - -e .
